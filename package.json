{
  "name": "dogtype",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "강아지 유형 세미나 도구 — Vite + React + TypeScript + Supabase",
  "scripts": {
    "config": "node scripts/gen-config.mjs",
    "verify": "node scripts/verify-questions.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "npm run config && npm run verify && vite",
    "build": "npm run config && npm run verify && npm run typecheck && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "^7.0.2",
    "vite": "^7.0.0"
  }
}
