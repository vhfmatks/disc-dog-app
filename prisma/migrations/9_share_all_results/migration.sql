-- 공유하면 그 스페이스의 결과 **전부**가 넘어갑니다. visible_from 게이트를 걷어냅니다.
--
-- 왜 바꾸나: 예전에는 수락 시각 이후에 제출된 결과만 넘겼습니다. 이미 제출한 사람이
-- 사후 공유를 예상하지 못했을 테니 빼두자는 뜻이었는데, 실제로 써보니 기존 스페이스
-- 둘을 연결하면 지도가 **텅 비어** 있었습니다. 어제 세미나와 오늘 세미나를 한 지도에서
-- 보자는 게 이 기능인데, 어제 것이 안 보이면 볼 게 없습니다.
--
-- ⚠ 무엇을 맞바꿨는지 분명히: 이건 **소급 노출**입니다. 공유를 맺는 순간, 그 전에
--   제출한 사람들의 닉네임과 강아지 유형이 상대 스페이스에 보입니다. 그 사람들은
--   제출할 때 이 공유를 몰랐습니다.
--
--   그래서 고지의 무게가 통째로 옮겨갔습니다. 참가 화면은 이제 공유가 걸려 있든
--   아니든 **모두에게** "진행자가 나중에 다른 스페이스와 공유하면 지금 낸 결과도
--   함께 보입니다"라고 미리 말합니다 (ParticipantApp). 그 문구가 이 결정의 전부입니다 —
--   지우지 마세요.

-- 컬럼을 지우면 이 컬럼을 걸고 있던 CHECK도 함께 사라진다.
alter table public.space_shares drop constraint if exists space_shares_visible_needs_accept;
alter table public.space_shares drop column if exists visible_from;

-- 공유가 언제 발효됐는지는 accepted_at이 그대로 들고 있습니다. visible_from은 그것과
-- 같은 값을 복사해 두던 것뿐이라, 게이트가 사라진 지금은 남겨둘 이유가 없습니다.
-- (읽는 사람이 "이게 뭘 막고 있지?"를 묻게 되는 죽은 컬럼이 더 나쁩니다.)
